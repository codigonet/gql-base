{
  "name": "gql-base",
  "version": "1.0.0",
  "description": "Base reference implementation for GraphQL components",
  "main": "index.js",
  "scripts": {
    "compile": "rimraf dist && tsc && tsc-alias",
    "debug": "npm run compile && npm run start",
    "start": "nodemon dist/index.js"
  },
  "keywords": [
    "GraphQL Base",
    "GraphQL"
  ],
  "author": "Cristian Acuña <cas@codigonet.cloud>",
  "licence":"MIT",
  "dependencies": {
    "@apollo/server": "^4.7.1",
    "graphql": "^16.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.2.1",
    "nodemon": "^2.0.22",
    "rimraf": "^5.0.1",
    "tsc-alias": "^1.8.6",
    "typescript": "^5.0.4"
  }
}
